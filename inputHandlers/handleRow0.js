const maxApi = require('max-api')
const noteValues = require('../configurations/noteValues')
const getMsPerNote = require('../utils/getMsPerNote')

const handleRow0 = (x, y, led, masterConfig) => {
  
}